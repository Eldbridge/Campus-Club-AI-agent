核心改进

采用多页面模块化设计，新增侧边栏导航，分为对外咨询、内部知识库管理、内部协作助手三个模块

优化整体 UI 布局，增加快捷提问按钮、页面标题、图标与宽屏展示

新增管理员密码验证功能，仅授权人员可进入后台编辑知识库

支持在后台直接编辑并保存 chuangxie_knowledge.txt，修改后实时生效

新增清空对话历史功能，提升使用体验

保留 v4 知识库本地实时监测更新机制，无需重启项目即可加载最新内容

保留完整异常处理与面向对象结构，系统更稳定、易扩展

存在不足

内部协作助手模块功能仍在开发中

未采用数据库存储数据

未接入 RAG 检索增强技术

UI 界面仍需持续优化升级
使用说明

1. 安装依赖：pip install -r requirements.txt

2. 在 Streamlit secrets 中配置 API_KEY、ENDPOINT_ID、ADMIN_PASSWORD

3. 启动项目：streamlit run app_v5.py

4. 对外页面可直接咨询问题；知识库管理需输入管理员密码进入编辑

5. 编辑保存后知识库自动更新，下一轮对话立即生效
