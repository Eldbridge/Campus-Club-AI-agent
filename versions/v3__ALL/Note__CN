# v3 异常处理版
## 核心改进
- 全面接入`try-except`异常捕获机制，覆盖API调用、文件读取、用户输入等场景
- 增加友好的错误提示信息，避免程序直接崩溃
- 提升系统健壮性与用户体验，故障时可正常提示而非闪退
- 保留v2的OOP结构与UI布局，仅增强稳定性

## 存在不足
- 知识库仍硬编码，未抽离为独立文件
- 未实现知识库热更新，修改后需重启服务
- 未做日志记录，不便排查问题

## 使用说明
1. 安装依赖：`pip install -r requirements.txt`
2. 在配置类中填写你的大模型API密钥
3. 启动项目：`streamlit run app_v3.py`
